class KaiserData {
    constructor(definitions, items, mdxFiles, assets) {
        
        this.definitions = definitions;
        this.items = items;
        this.mdxFiles = mdxFiles;
        this.assets = assets;

         /* Prepare Navigation Sidebar data */

        //Create flat array of all pageNodes

        //Place children with parent pages

        //Build navigation template-usable array

        //Flatten nav tree for paging


    
    }

    populateMDX (compileFn) {
        
    }
}

exports.KaiserData = KaiserData;